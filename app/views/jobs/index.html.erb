

<div class="pull-right">
  <%= link_to("Add a job", new_job_path, :class => "btn btn-default" ) %>
</div>

<div class="dropdown clearfix pull-right">
    <button class="btn btn-default dropdown-toggle" type="button" id="dropdownMenuDivider" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
      Sort
        <span class="caret"></span>
    </button>
    <ul class="dropdown-menu" aria-labelledby="dropdownMenuDivider">
        <li>
          <%= link_to("Salary Min.", jobs_path(:order => "by_lower_bound")) %>
        </li>
        <li>
            <%= link_to("Salary Max.", jobs_path(:order => "by_upper_bound")) %>

        </li>
        <li>
          <%= link_to("Lastest", jobs_path ) %>

        </li>
    </ul>
</div>

<table class="table table-boldered">
    <thead id="main-content">
        <tr>
            <th>Job Title</th>
            <th>Salary Range</th>
            <th>Salary Min.</th>
            <th>Salary Max.</th>
            <th>Posted Date</th>
        </tr>

    </thead>
    <tbody id="main-content">
        <% @jobs.each do |job| %>
        <tr>
            <td>
                <%= link_to(job.title, job_path(job)) %>
            </td>
            <td>
                <%= job.wage_lower_bound %>
            </td>
            <td>
                <%= job.wage_upper_bound %>
            </td>
            <td>
                <%= job.created_at.strftime("%m-%d") %>
            </td>
        </tr>
        <% end %>
    </tbody>

</table>
